<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PersonalizArts - Produtos para Sublimação</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .hero-gradient {
            background: linear-gradient(135deg, #f5f5f5 0%, #e8e8e8 100%);
        }
        .product-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }
    </style>
</head>
<body class="bg-white">
    <!-- Header -->
	
	<!-- Topo -->
	 <header class="bg-white shadow">
		<div class="max-w-7xl mx-auto px-4">
			<div class="flex justify-between items-center py-3">
				<div class="flex items-center space-x-2">
					<a href="/"><img src="./../../img/logo.png" alt="Logo" class="h-10"></a>
				</div>
				<%- include('./partials/topo.ejs') %>
			</div>
			<%- include('./partials/menu.ejs') %>
	</header>
  
    

    <!-- Hero Section -->
    <section id="home" class="relative py-24 overflow-hidden">
        <!-- Background Banner -->
        <div class="absolute inset-0 bg-cover bg-center bg-no-repeat" 
             style="background-image: url('./img/hero-banner-sublimation.png');">
        </div>
        
        <!-- Overlay -->
        <div class="absolute inset-0 bg-black bg-opacity-40"></div>
        
        <!-- Content -->
        <div class="relative z-10 container mx-auto px-4">
            <div class="text-center text-white" >
                <h1 class="text-5xl md:text-7xl font-bold mb-6">
                    Brindes, Personalizados e Materiais Gráficos<br>
                    
                </h1>
                <p class="text-xl md:text-2xl mb-8 max-w-2xl mx-auto">
                    Mais de 5.000 produtos com qualidade garantida
                </p>
                

                <!--
                <div class="flex flex-wrap gap-3 justify-center">
                    <button class="bg-blue-500 text-white px-6 py-3 rounded-lg hover:bg-blue-600 transition">
                        <i class="fas fa-eye mr-2"></i>Ver Itens
                    </button>
                    <button class="bg-pink-500 text-white px-6 py-3 rounded-lg hover:bg-pink-600 transition">
                        <i class="fas fa-heart mr-2"></i>Favoritos
                    </button>
                    <button class="bg-yellow-500 text-white px-6 py-3 rounded-lg hover:bg-yellow-600 transition">
                        <i class="fas fa-star mr-2"></i>Novidades
                    </button>
                    <button class="bg-green-500 text-white px-6 py-3 rounded-lg hover:bg-green-600 transition">
                        <i class="fas fa-percentage mr-2"></i>Promoção
                    </button>
                </div>-->
            </div>
        </div>
    </section>

    <!-- Products Section -->
    <section id="products" class="py-16 bg-gray-50">
        <div class="container mx-auto px-4">
            <h2 class="text-3xl font-bold text-center mb-12">Materiais Gráficos</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
                <!-- Product 1 -->
                <!--<div class="product-card bg-white rounded-lg shadow-md overflow-hidden cursor-pointer" onclick="window.open('/produto/1', '_blank')">-->
                <% produtos.forEach(prods=> { %>
					<% if (prods.lancamentos == "1") { %>
						<a href="/produto/<%= prods.id %>">
							<div class="product-card bg-white rounded-lg shadow-md overflow-hidden cursor-pointer flex flex-col" style="min-height: 345px;">
								<img src="./img/produtos/<%= prods.imagem_principal %>" alt="" class="w-full h-48 object-cover">
								<div class="flex flex-col justify-between flex-1 p-4">
									<h3 class="font-semibold text-gray-800 mb-4"><%= prods.titulo %></h3>
									<div class="flex items-center justify-between mt-auto">
										<span class="text-lg font-bold text-green-600">R$ <%= prods.valor %></span>
										<button class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition" onclick="event.stopPropagation();">
											<i class="fas fa-cart-plus"></i>
										</button>
									</div>
								</div>
							</div>
						</a>
					<% } %>
                <% }) %>
               
            </div>
        </div>
    </section>

    <!-- Featured Products
    <section id="featured" class="py-16">
        <div class="container mx-auto px-4">
            <h2 class="text-3xl font-bold text-center mb-12">Destaques</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
                <% produtos.forEach(prods=> { %>
					<% if (prods.destaques == "1") { %>
						<a href="/produto/<%= prods.id %>">
							<div class="product-card bg-white rounded-lg shadow-md overflow-hidden cursor-pointer flex flex-col" style="min-height: 345px;">
								<img src="./img/produtos/<%= prods.imagem %>" alt="" class="w-full h-48 object-cover">
								<div class="flex flex-col justify-between flex-1 p-4">
									<h3 class="font-semibold text-gray-800 mb-4"><%= prods.titulo %></h3>
									<div class="flex items-center justify-between mt-auto">
										<span class="text-lg font-bold text-green-600">R$ <%= prods.valor %></span>
										<button class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition" onclick="event.stopPropagation();">
											<i class="fas fa-cart-plus"></i>
										</button>
									</div>
								</div>
							</div>
						</a>
					<% } %>
                <% }) %>
            </div>
        </div>
    </section>
	 -->
    <!-- Banner Section -->
    <section class="py-8 relative overflow-hidden">
        <!-- Background Image -->
        <div class="absolute inset-0 bg-cover bg-center bg-no-repeat" 
             style="background-image: url('pGnTljO0c39tVbVaRLpPc');">
        </div>
        
        <!-- Overlay for better text readability -->
        <div class="absolute inset-0 bg-black bg-opacity-30"></div>
        
        <!-- Content -->
         <div class="relative z-10 container mx-auto px-4">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 text-white">
                <a href="/brindes"><div class="bg-white bg-opacity-20 backdrop-blur-sm rounded-lg p-6" 
				style="background-image:url('./img/banner_grafica.jpg');min-height:350px;">
                    <h3 class="text-2xl font-bold mb-4" style="color: #000;font-size: 33px;margin-left: 101px;">BRINDES E PERSONALIZADOS</h3>
                    <p class="text-lg" style="color: #000;font-size: 25px;margin-left: 150px;">Melhor qualidade do mercado</p>
					<p class="text-lg" style="color: #000;font-size: 20px;margin-left: 240px;margin-top: 10px;font-weight: 900;">Confira Aqui...</p>
                </div>
				</a>
                <a href="/grafica">
					<div class="bg-white bg-opacity-20 backdrop-blur-sm rounded-lg p-6" 
						style="background-image:url('./img/banner_brindes.jpg');
						min-height:350px;
						color:#000;
						">
						<h3 class="text-2xl font-bold mb-4" style="font-size:30px;">MATERIAIS GRÁFICOS</h3>
						<p class="text-lg" style="font-size: 21px;margin-top: -15px;">Para sublimação profissional</p>
						<p class="text-lg" style="color: #000;font-size: 20px;margin-left: 0px;margin-top: 10px;font-weight: 900;">Confira Aqui...</p>
					</div>
				</a>
            </div>
        </div>
    </section>



    <!-- Footer -->
       <%- include('./partials/footer.ejs') %>

    <script>
        // Simple cart functionality
        let cart = [];
        
        document.querySelectorAll('.fa-cart-plus').forEach(button => {
            button.addEventListener('click', function() {
                // Simple animation
                this.style.transform = 'scale(1.2)';
                setTimeout(() => {
                    this.style.transform = 'scale(1)';
                }, 200);
                
                // Update cart count (simplified)
                const cartCount = document.querySelector('.fa-shopping-cart + span');
                const currentCount = parseInt(cartCount.textContent);
                cartCount.textContent = currentCount + 1;
            });
        });
        
        // Mobile menu toggle
        const mobileMenuBtn = document.getElementById('mobile-menu-btn');
        const mobileMenu = document.getElementById('mobile-menu');
        
        mobileMenuBtn.addEventListener('click', function() {
            mobileMenu.classList.toggle('hidden');
            const icon = this.querySelector('i');
            if (mobileMenu.classList.contains('hidden')) {
                icon.className = 'fas fa-bars';
            } else {
                icon.className = 'fas fa-times';
            }
        });
        
        // Smooth scrolling for navigation links
        document.querySelectorAll('nav a[href^="#"]').forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                const targetElement = document.querySelector(targetId);
                
                if (targetElement) {
                    targetElement.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
                
                // Close mobile menu after clicking
                if (window.innerWidth < 768) {
                    mobileMenu.classList.add('hidden');
                    mobileMenuBtn.querySelector('i').className = 'fas fa-bars';
                }
                
                // Simple click effect
                this.style.color = '#3b82f6';
                setTimeout(() => {
                    this.style.color = '';
                }, 300);
            });
        });
    </script>
</body>
</html>