<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout - PersonalizArts</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .hero-gradient {
            background: linear-gradient(135deg, #f5f5f5 0%, #e8e8e8 100%);
        }
        .checkout-step {
            transition: all 0.3s ease;
        }
        .checkout-step.active {
            border-color: #3b82f6;
            background-color: #eff6ff;
        }
        .form-group {
            transition: all 0.3s ease;
        }
        .form-group:focus-within {
            transform: translateY(-2px);
        }
        .payment-method {
            transition: all 0.3s ease;
            cursor: pointer;
        }
        .payment-method:hover {
            border-color: #3b82f6;
        }
        .payment-method.selected {
            border-color: #3b82f6;
            background-color: #eff6ff;
        }
    </style>
</head>
<body class="bg-gray-50">
    <!-- Header -->
	<header class="bg-white shadow">
		<div class="max-w-7xl mx-auto px-4">
			<div class="flex justify-between items-center py-3">
				<div class="flex items-center space-x-2">
					<a href="/"><img src="./../../img/logo.png" alt="Logo" class="h-10"></a>
				</div>
				<%- include('./partials/topo.ejs') %>
			</div>
			<%- include('./partials/menu.ejs') %>
	</header>
    <!-- Breadcrumb -->
    <section class="bg-gray-100 py-4">
        <div class="container mx-auto px-4">
            <!--<nav class="text-sm">
                <a href="#" class="text-blue-600 hover:underline">Início</a>
                <span class="mx-2">/</span>
                <a href="#" class="text-blue-600 hover:underline">Carrinho</a>
                <span class="mx-2">/</span>
                <span class="text-gray-600">Checkout</span>
            </nav>-->
        </div>
        
    </section>

    <!-- Checkout Progress 
    <section class="py-6 bg-white border-b">
        <div class="container mx-auto px-4">
            <div class="flex items-center justify-center space-x-8">
                <div class="checkout-step active flex items-center space-x-2 px-4 py-2 rounded-lg border-2">
                    <div class="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center text-sm font-bold">1</div>
                    <span class="font-medium text-blue-600">Dados Pessoais</span>
                </div>
                <div class="w-8 h-1 bg-gray-300"></div>
                <div class="checkout-step flex items-center space-x-2 px-4 py-2 rounded-lg border-2 border-gray-300">
                    <div class="w-8 h-8 bg-gray-300 text-gray-600 rounded-full flex items-center justify-center text-sm font-bold">2</div>
                    <span class="font-medium text-gray-600">Pagamento</span>
                </div>
                <div class="w-8 h-1 bg-gray-300"></div>
                <div class="checkout-step flex items-center space-x-2 px-4 py-2 rounded-lg border-2 border-gray-300">
                    <div class="w-8 h-8 bg-gray-300 text-gray-600 rounded-full flex items-center justify-center text-sm font-bold">3</div>
                    <span class="font-medium text-gray-600">Confirmação</span>
                </div>
            </div>
        </div>
    </section>-->

    <!-- Checkout Content -->
    <section class="py-12">
        <div class="container mx-auto px-4">
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
                <!-- Checkout Form -->
                <div class="lg:col-span-2">
                    <div class="bg-white rounded-lg shadow-md p-8">
                        <h2 class="text-2xl font-bold text-gray-800 mb-8">
                            <i class="fas fa-user mr-2 text-blue-600"></i>
                            Dados Pessoais
                        </h2>
                        
                        <form id="checkout-form">
                            <!-- Personal Information -->
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                                <div class="form-group">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">Nome Completo *</label>
                                    <input type="text" required 
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" id="nome"
                                           placeholder="Digite seu nome completo">
                                </div>
                                <div class="form-group">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">CPF *</label>
                                    <input type="text" required  
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" id="cpf"
                                           placeholder="000.000.000-00">
                                </div>
                                <div class="form-group">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">E-mail *</label>
                                    <input type="email" required 
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" id="email"
                                           placeholder="seu@email.com">
                                </div>
                                <div class="form-group">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">Telefone *</label>
                                    <input type="tel" required 
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" id="phone" 
                                           placeholder="(11) 99999-9999">
                                </div>
                            </div>

                            <!-- Address Information -->
                            <h3 class="text-xl font-semibold text-gray-800 mb-6">
                                <i class="fas fa-map-marker-alt mr-2 text-blue-600"></i>
                                Endereço de Entrega
                            </h3>
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
								<div class="mb-8">
									<label class="flex items-start space-x-3">
										<input type="checkbox" id="retirar_pedido" class="mt-1 w-5 h-5 text-blue-600 border-gray-300 rounded focus:ring-blue-500">
										<span class="text-lg font-bold text-blue-600">
											Irei Retirar o Pedido
										</span>
									</label>
								</div>
							</div>
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
                                <div class="form-group">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">CEP *</label>
                                    <input type="text" id="cep" 
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                           placeholder="00000-000">
                                </div>
                                <div class="form-group md:col-span-2">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">Rua *</label>
                                    <input type="text" id="rua" 
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                           placeholder="Nome da rua">
                                </div>
                            </div>

                            <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-6">
                                <div class="form-group">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">Número *</label>
                                    <input type="text"  id="numero"
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                           placeholder="123">
                                </div>
                                <div class="form-group">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">Complemento</label>
                                    <input type="text" id="complemento"
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                           placeholder="Apto, Bloco">
                                </div>
                                <div class="form-group md:col-span-2">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">Bairro *</label>
                                    <input type="text" id="bairro" 
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                           placeholder="Nome do bairro">
                                </div>
                            </div>

                            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                                <div class="form-group">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">Cidade *</label>
                                    <input type="text"  id="cidade" 
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                                           placeholder="Nome da cidade">
                                </div>
                                <div class="form-group">
                                    <label class="block text-sm font-medium text-gray-700 mb-2">Estado *</label>
                                    <select  id="uf" 
                                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                                        <option value="">Selecione o estado</option>
                                        <option value="SP">São Paulo</option>
                                        <option value="RJ">Rio de Janeiro</option>
                                        <option value="MG">Minas Gerais</option>
                                        <option value="RS">Rio Grande do Sul</option>
                                        <option value="PR">Paraná</option>
                                        <option value="SC">Santa Catarina</option>
                                        <option value="BA">Bahia</option>
                                        <option value="GO">Goiás</option>
                                        <option value="PE">Pernambuco</option>
                                        <option value="CE">Ceará</option>
                                    </select>
                                </div>
                            </div>

                            <!-- Payment Method -->
                            <h3 class="text-xl font-semibold text-gray-800 mb-6">
                                <i class="fas fa-credit-card mr-2 text-blue-600"></i>
                                Forma de Pagamento
                            </h3>
							<div class="mb-8">
									<label class="flex items-start space-x-3">
										<input type="checkbox" id="pagto_cartao" class="mt-1 w-5 h-5 text-blue-600 border-gray-300 rounded focus:ring-blue-500">
										<span class="text-lg font-bold text-blue-600">
											Cartão de Crédito
										</span>
									</label>
								</div>
								<div class="mb-8">
									<label class="flex items-start space-x-3">
										<input type="checkbox" id="pagto_pix" checked class="mt-1 w-5 h-5 text-blue-600 border-gray-300 rounded focus:ring-blue-500">
										<span class="text-lg font-bold text-blue-600">
											Pix
										</span>
									</label>
								</div>
                            

                            
                           

                            <!-- Terms -->
                            <div class="mb-8">
                                <label class="flex items-start space-x-3">
                                    <input type="checkbox" required class="mt-1 w-5 h-5 text-blue-600 border-gray-300 rounded focus:ring-blue-500">
                                    <span class="text-sm text-gray-700">
                                        Eu aceito os <a href="#" class="text-blue-600 hover:underline">termos de uso</a> 
                                        e <a href="#" class="text-blue-600 hover:underline">política de privacidade</a>
                                    </span>
                                </label>
                            </div>

                            <!-- Submit Button -->
                            <button type="submit" class="w-full bg-green-600 text-white py-4 px-6 rounded-lg hover:bg-green-700 transition font-medium text-lg">
                                <i class="fas fa-lock mr-2"></i>
                                Finalizar Pedido
                            </button>
                        </form>
                    </div>
                </div>

                <!-- Order Summary -->
                <div class="lg:col-span-1">
                    <div class="bg-white rounded-lg shadow-md p-6 sticky top-4">
                        <h3 class="text-xl font-bold text-gray-800 mb-6">Resumo do Pedido</h3>
                        <% produtos.forEach(dados=> { %>
                        <!-- Product Item -->
                        <div class="flex items-center space-x-4 mb-6 pb-6 border-b">
                            <img src="https://cdn-icons-png.flaticon.com/512/2652/2652218.png" alt="" class="w-16 h-16 object-cover rounded-lg">
                            <div class="flex-1">
                                <input type="hidden" id="id_produto" value="<%= dados.id %>"></Input>
								<input type="hidden" id="descricao_produto" value="<%= dados.titulo %>"></Input>
								<input type="hidden" id="qtde_produto" value="<%= qtde %>"></Input>
								<input type="hidden" id="valor_produto" value="<%= dados.valor %>"></Input>
								
								<h4 class="font-semibold text-gray-800"><%= dados.titulo %></h4>
                                <p class="text-sm text-gray-600">Quantidade: <%= qtde %></p>
                                <p class="text-lg font-bold text-green-600">R$ <%= dados.valor %></p>
                            </div>
                        </div>
                        <% }) %>
                        <!-- Order Totals -->
                        <div class="space-y-3 mb-6">
                            <!--
                            <div class="flex justify-between">
                                <span class="text-gray-600">Subtotal:</span>
                                <span class="font-medium">R$ <%= subtotal %></span>
                            </div>
                            
                            <div class="flex justify-between">
                                <span class="text-gray-600">Frete:</span>
                                <span class="font-medium text-green-600">Grátis</span>
                            </div>
                            <div id="discount-line" class="flex justify-between hidden">
                                <span class="text-gray-600">Desconto PIX (5%):</span>
                                <span class="font-medium text-green-600">-R$ 0,63</span>
                            </div>-->
                            <div class="pt-3">
                                <div class="flex justify-between text-lg font-bold">
                                    <span>Total:</span>
                                    <span id="total-price" class="text-green-600"><%= subtotal %></span>
									<input type="hidden" id="total_produto" value="<%= subtotal %>"></Input>
                                </div>
                            </div>
                        </div>

                        <!-- Security Icons 
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <div class="flex items-center justify-center space-x-4 text-gray-600">
                                <div class="text-center">
                                    <i class="fas fa-shield-alt text-xl mb-1"></i>
                                    <p class="text-xs">Compra Segura</p>
                                </div>
                                <div class="text-center">
                                    <i class="fas fa-truck text-xl mb-1"></i>
                                    <p class="text-xs">Entrega Rápida</p>
                                </div>
                                <div class="text-center">
                                    <i class="fas fa-undo text-xl mb-1"></i>
                                    <p class="text-xs">Troca Fácil</p>
                                </div>
                            </div>
                        </div>-->

                        <!-- Support Contact -->
                        <div class="mt-6 text-center">
                            <p class="text-sm text-gray-600 mb-2">Precisa de ajuda?</p>
                            <div class="space-y-1">
                                <p class="text-sm">
                                    <i class="fas fa-phone mr-2 text-blue-600"></i>
                                    (12) 98800-5619
                                </p>
                                <!--<p class="text-sm">
                                    <i class="fas fa-envelope mr-2 text-blue-600"></i>
                                    contato@personalizarts.com
                                </p>-->
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-8">
        <div class="container mx-auto px-4">
            <div class="text-center">
                <div class="flex justify-center space-x-8 mb-4">
                    <i class="fas fa-shield-alt text-2xl"></i>
                    <i class="fab fa-ssl text-2xl"></i>
                    <i class="fas fa-lock text-2xl"></i>
                </div>
                <p class="text-gray-400">© 2024 PersonalizArts. Compra 100% segura e protegida.</p>
            </div>
        </div>
    </footer>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script>
	
		$(document).ready(function () {
			$("#cep").blur(function () {
            //Nova variável "cep" somente com dígitos.
            var cep = $(this).val().replace(/\D/g, "");
    
            //Verifica se campo cep possui valor informado.
            if (cep != "") {
              //Expressão regular para validar o CEP.
              var validacep = /^[0-9]{8}$/;
    
              //Valida o formato do CEP.
              if (validacep.test(cep)) {
                //Preenche os campos com "..." enquanto consulta webservice.
                $("#rua").val("...");
                $("#bairro").val("...");
                $("#cidade").val("...");
                $("#uf").val("...");
                $("#ibge").val("...");
    
                //Consulta o webservice viacep.com.br/
                $.getJSON(
                  "https://viacep.com.br/ws/" + cep + "/json/?callback=?",
                  function (dados) {
                    if (!("erro" in dados)) {
                      //Atualiza os campos com os valores da consulta.
                      $("#rua").val(dados.logradouro);
                      $("#bairro").val(dados.bairro);
                      $("#cidade").val(dados.localidade);
                      $("#uf").val(dados.uf);
                      $("#ibge").val(dados.ibge);
					  $("#numero").focus();
                    } //end if.
                    else {
                      //CEP pesquisado não foi encontrado.
                      //limpa_formulário_cep();
                      alert("CEP não encontrado.");
                    }
                  }
                );
              } //end if.
              else {
                //cep é inválido.
                //limpa_formulário_cep();
                alert("Formato de CEP inválido.");
              }
            } //end if.
            else {
              //cep sem valor, limpa formulário.
              //limpa_formulário_cep();
            }
          })
		}) 
		document.addEventListener("DOMContentLoaded", function () {
			
			//const produtos = JSON.parse(localStorage.getItem("produtos")) || [];
			//const produto = produtos.find(p => p.id_produto === id_produto);

			//console.log(produtos)
			/*if (produtos) {
				document.getElementById("quantidade").value = produtos.qtde;
			}*/
			
			
		});
		
		const cartao = document.getElementById("pagto_cartao");
		const pix = document.getElementById("pagto_pix");

		cartao.addEventListener("change", () => {
			
			if (cartao.checked) {
			  pix.checked = false;
			}
		});

		pix.addEventListener("change", () => {
			if (pix.checked) {
			  cartao.checked = false;
			}
		});
		
		
        // Mobile menu toggle
        const mobileMenuBtn = document.getElementById('mobile-menu-btn');
        const mobileMenu = document.getElementById('mobile-menu');
        
        if (mobileMenuBtn && mobileMenu) {
            mobileMenuBtn.addEventListener('click', function() {
                mobileMenu.classList.toggle('hidden');
                const icon = this.querySelector('i');
                if (mobileMenu.classList.contains('hidden')) {
                    icon.className = 'fas fa-bars';
                } else {
                    icon.className = 'fas fa-times';
                }
            });
        }

        // Input masks
        function maskCPF(value) {
            return value
                .replace(/\D/g, '')
                .replace(/(\d{3})(\d)/, '$1.$2')
                .replace(/(\d{3})(\d)/, '$1.$2')
                .replace(/(\d{3})(\d{1,2})/, '$1-$2')
                .replace(/(-\d{2})\d+?$/, '$1');
        }

        function maskPhone(value) {
            return value
                .replace(/\D/g, '')
                .replace(/(\d{2})(\d)/, '($1) $2')
                .replace(/(\d{4})(\d)/, '$1-$2')
                .replace(/(\d{4})-(\d)(\d{4})/, '$1$2-$3')
                .replace(/(-\d{4})\d+?$/, '$1');
        }

        function maskCEP(value) {
            return value
                .replace(/\D/g, '')
                .replace(/(\d{5})(\d)/, '$1-$2')
                .replace(/(-\d{3})\d+?$/, '$1');
        }

        function maskCardNumber(value) {
            return value
                .replace(/\D/g, '')
                .replace(/(\d{4})(\d)/, '$1 $2')
                .replace(/(\d{4})(\d)/, '$1 $2')
                .replace(/(\d{4})(\d)/, '$1 $2')
                .replace(/(\d{4})\d+?$/, '$1');
        }

        function maskCardExpiry(value) {
            return value
                .replace(/\D/g, '')
                .replace(/(\d{2})(\d)/, '$1/$2')
                .replace(/(\d{2}\/\d{2})\d+?$/, '$1');
        }

        // Apply masks
        document.getElementById('cpf').addEventListener('input', function(e) {
            e.target.value = maskCPF(e.target.value);
        });

        document.getElementById('phone').addEventListener('input', function(e) {
            e.target.value = maskPhone(e.target.value);
        });

        document.getElementById('cep').addEventListener('input', function(e) {
            e.target.value = maskCEP(e.target.value);
        });

        /*document.getElementById('card-number').addEventListener('input', function(e) {
            e.target.value = maskCardNumber(e.target.value);
        });
	
        document.getElementById('card-expiry').addEventListener('input', function(e) {
            e.target.value = maskCardExpiry(e.target.value);
        });

        document.getElementById('card-cvv').addEventListener('input', function(e) {
            e.target.value = e.target.value.replace(/\D/g, '').substring(0, 3);
        });
		*/
		

		
        // CEP lookup
        /*document.getElementById('cep').addEventListener('blur', function(e) {
            const cep = e.target.value.replace(/\D/g, '');
            if (cep.length === 8) {
                fetch(`https://viacep.com.br/ws/${cep}/json/`)
                    .then(response => response.json())
                    .then(data => {
                        if (!data.erro) {
                            document.getElementById('street').value = data.logradouro;
                            document.getElementById('neighborhood').value = data.bairro;
                            document.getElementById('city').value = data.localidade;
                            document.getElementById('state').value = data.uf;
                        }
                    })
                    .catch(error => console.log('Error:', error));
            }
        });
		*/
        // Payment method selection
        function selectPayment(method) {
            // Remove active class from all payment methods
            document.querySelectorAll('.payment-method').forEach(el => {
                el.classList.remove('selected');
            });
            
            // Add active class to selected method
            event.currentTarget.classList.add('selected');
            
            // Hide all payment forms
            document.getElementById('credit-card-form').classList.add('hidden');
            document.getElementById('pix-form').classList.add('hidden');
            document.getElementById('discount-line').classList.add('hidden');
            
            // Show relevant payment form
            if (method === 'credit' || method === 'debit') {
                document.getElementById('credit-card-form').classList.remove('hidden');
                document.getElementById('total-price').textContent = 'R$ 12,57';
            } else if (method === 'pix') {
                document.getElementById('pix-form').classList.remove('hidden');
                document.getElementById('discount-line').classList.remove('hidden');
                document.getElementById('total-price').textContent = 'R$ 11,94';
            }
        }
		const checkbox = document.getElementById('retirar_pedido');
		const inputs = [document.getElementById("cep"), document.getElementById("rua"),document.getElementById("bairro"),document.getElementById("cidade"),document.getElementById("uf"),document.getElementById("numero"),document.getElementById("complemento")];
		
		checkbox.addEventListener("change", function () {
			inputs.forEach(input => {
				input.disabled = this.checked; // se marcado -> desabilita
				input.value="";
			});
		});
  
  
  
		
        // Form submission
        document.getElementById('checkout-form').addEventListener('submit', async function(e) {
			
            e.preventDefault();
            let retirar = document.getElementById('retirar_pedido');
			
			/*
			let cartao = document.getElementById("pagto_cartao");
			let pix = document.getElementById("pagto_pix");
						
			let Nome = document.getElementById('nome').value;
			let Cpf = document.getElementById('cpf').value;
			let Telefone = document.getElementById('phone').value;
			let Email = document.getElementById('email').value;
			
			
			
			
			let	Cep = document.getElementById('cep').value;
			let	Rua=document.getElementById('rua').value;
			let	Numero=document.getElementById('numero').value;
			let	Complemento=document.getElementById('complemento').value;
			let	Bairro=document.getElementById('bairro').value;
			let	Cidade=document.getElementById('cidade').value;
			let	Uf=document.getElementById('uf').value;
			
			let id_produto = document.getElementById('id_produto').value;
			let descricao_produto = document.getElementById('descricao_produto').value;
			let qtde_produto = document.getElementById('qtde_produto').value;
			let valor_produto = document.getElementById('valor_produto').value;
			let total_produto = document.getElementById('total_produto').value;
			
			//let	Produto=document.getElementById('produto').value;
			//let	Qtde=document.getElementById('qtde').value;
			//let	Valor=document.getElementById('valor').value;
			
			
			let forma_pagto = "";
			if(pix.checked==true){
				forma_pagto = "pix";
			}
			if(cartao.checked==true){
				forma_pagto = "cartao";
			}
			*/
			
			// monta objeto com os valores
		  const pedido = {
			forma_pagto: document.getElementById("pagto_pix").checked ? "pix" : "cartao",
			envio: document.getElementById("retirar_pedido").checked ? "Retirar Pedido" : "Enviar",
			nome: document.getElementById("nome").value,
			cpf: document.getElementById("cpf").value,
			telefone: document.getElementById("phone").value,
			email: document.getElementById("email").value,
			cep: document.getElementById("cep").value,
			rua: document.getElementById("rua").value,
			numero: document.getElementById("numero").value,
			complemento: document.getElementById("complemento").value,
			bairro: document.getElementById("bairro").value,
			cidade: document.getElementById("cidade").value,
			uf: document.getElementById("uf").value,
			id_produto: document.getElementById("id_produto").value,
			descricao_produto: document.getElementById("descricao_produto").value,
			qtde_produto: document.getElementById("qtde_produto").value,
			valor_produto: document.getElementById("valor_produto").value,
			total_produto: document.getElementById("total_produto").value
		  };
			
		  try {
			const response = await fetch("https://personalizarts.com.br/pedidos", {
			  method: "POST",
			  headers: {
				"Content-Type": "application/json"
			  },
			  body: JSON.stringify(pedido)
			});
			
			console.log(response);
			if (response.ok) {
				const data = await response.json();
				const url = "https://n8n.centralatende.com.br/webhook/987040af-1c1f-48a2-9f61-f0c2dbdc5416";
				alert("Pedido enviado com sucesso! Estaremos Entrando em Contato pelo número informado..");
				
				//const url = "https://n8n.centralatende.com.br/webhook-test/987040af-1c1f-48a2-9f61-f0c2dbdc5416";
				
				fetch(url, {
					method: "POST",
					headers: {
						"Content-Type": "application/json"
					},
					body: JSON.stringify(pedido)
				})
			  .then(response => response.json())
			  .then(result => console.log("Sucesso:", result))
			  .catch(error => console.error("Erro:", error));
			  
			  // Redireciona imediatamente
			 window.location.href = "/";

			} else {
			  alert("Erro ao enviar pedido");
			}
		  } catch (err) {
			console.error(err);
			alert("Falha na conexão com o servidor");
		  }
			return;
			console.log(Nome);
			console.log(Cpf);
			console.log(Telefone);
			console.log(Cep);
			console.log(Rua);
			console.log(Numero);
			console.log(Complemento);
			console.log(Bairro);
			console.log(Cidade);
			console.log(Uf);
			console.log(id_produto);
			console.log(descricao_produto);
			console.log(qtde_produto);
			console.log(valor_produto);
			console.log(total_produto);
			console.log(forma_pagto)
			
			return;
			
			
			//https://n8n.centralatende.com.br/webhook/987040af-1c1f-48a2-9f61-f0c2dbdc5416
			
			let retirar_pedido = document.getElementById('retirar_pedido');
			
			if (checkbox.checked) {
				alert(checkbox.checked)
			} else {
				alert(checkbox.checked)
			}
			
			const data = {
				  Nome: "Manasses Hdson",
				  CPF: "052.343.456-10",
				  Email: "manasseshudson@gmail.com",
				  Telefone: "(32)99148-7863",
				  Cep: "",
				  Rua: "",
				  Numero: "",
				  Complemento: "",
				  Bairro: "",
				  Cidade: "",
				  Uf: "",
				  Produto: "Kit Caixa MDF Personalizada com Caderno Kraft + Caneta Touch Personalizada + 2 Copos de Fibra de Bambu",
				  Qtde: "5",
				  Valor: "324,50"
			};
			alert(JSON.stringify(data));
			
			
			//const url = "https://n8n.centralatende.com.br/webhook/987040af-1c1f-48a2-9f61-f0c2dbdc5416";
			const url = "/pedidos";

			

			fetch(url, {
			  method: "POST",
			  headers: {
				"Content-Type": "application/json"
			  },
			  body: JSON.stringify(data)
			})
			  .then(response => response.json())
			  .then(result => console.log("Sucesso:", result))
			  .catch(error => console.error("Erro:", error));

			
            // Simple validation
            const requiredFields = this.querySelectorAll('[required]');
            let isValid = true;
            
            requiredFields.forEach(field => {
                if (!field.value.trim()) {
                    isValid = false;
                    field.classList.add('border-red-500');
                } else {
                    field.classList.remove('border-red-500');
                }
            });
            
            if (isValid) {
                // Show success message
                alert('Pedido realizado com sucesso! Você receberá um e-mail de confirmação.');
                
                // In a real application, you would submit the form data to your server
                console.log('Form submitted successfully');
            } else {
                alert('Por favor, preencha todos os campos obrigatórios.');
            }
        });
    </script>
</body>
</html>